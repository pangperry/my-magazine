<!DOCTYPE html>
<html class="no-js">
  <%= render 'head' %>
  <body class="<%=  controller.controller_name  %>">
    <%= render 'chromeframe' %>
    <div class="container">
      <%= render 'flashes' %>
      <%= yield %>
      <% # Javascript at the bottom for fast page loading %>
      <%= render 'javascripts' %>
    </div>
  </body>
</html>
